[project]
name = "cli-agent-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yash-931", email = "yaggarwal931@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "google-genai>=2.21.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
cli-agent-py = "cli_agent_py:run"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
